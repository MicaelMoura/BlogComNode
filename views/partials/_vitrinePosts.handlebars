{{!-- Últimas Postagens --}}
<h2>Postagens recentes: </h2>
{{#each postagens}}
    <div class="card mt-4 shadow">
        <div class="card-body m-2">
            <div class="card-title"><h3>{{titulo}}</h3></div>
            <div class="card-text">{{descricao}}</div>
            <br>
            <a href="/postagens/post={{slug}}"><button class="btn btn-success">Leia mais...</button></a>
        </div>
            <div class="card-footer mt-4"><i>Categoria: {{categoria.nome}} - {{data}}</i></div>
    </div>
{{else}}
    <h5 class="mt-4"> <i>Nenhuma categoria cadastrada!</i></h5>
{{/each}}
<br>